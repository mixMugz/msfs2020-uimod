window.document.addEventListener("keyup", function (e) {
  if (e.keyCode == hKey) {
    hToggler();
  }
  else if (e.keyCode == iKey) {
    iToggler();
  }
  else if (e.keyCode == mKey) {
    mToggler();
  }
});
